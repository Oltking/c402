# c402 - Brand

The official c402 logo. **Do not alter the proportions, colors, or reconstruct the glyph.**

## The mark
Code brackets `[ ]` (the protocol / an endpoint) enclosing a padlock (confidential compute).
It reads as "a call, made private."

| File | Use |
|---|---|
| `c402-mark.svg` | The mark, transparent, scalable - **primary source of truth** |
| `c402-mark-1024.png` · `-512` · `-256` · `-128` | Raster mark, transparent background |
| `c402-mark-onlight.svg` / `c402-icon-1024.png` | App icon: mark on a white square (favicon / tile) |
| `c402-wordmark.svg` | Horizontal lockup (mark + `c402`), scalable |
| `c402-lockup-square.svg` / `c402-lockup-1024.png` | Square lockup for avatars / OG / social |

## Colors
| Token | Hex | Use |
|---|---|---|
| Ink | `#0a0b0d` | brackets, wordmark |
| Accent (blue) | `#1d4ed8` | the lock |
| Line | `#e4e6ea` | hairline borders / icon frame |
| Surface | `#ffffff` | background |

## Wordmark
Lowercase **`c402`**, monospace (`ui-monospace, SF Mono, Menlo`), weight 600, letter-spacing −0.5.
Always lowercase. The mark may be used alone; the wordmark should keep the mark to its left.

## Don'ts
- Don't recolor the lock anything but the accent blue.
- Don't add gradients, shadows, or rounded corners (the brand is sharp-cornered).
- Don't stretch, rotate, or crowd the mark (keep clear space ≈ one bracket-width).
